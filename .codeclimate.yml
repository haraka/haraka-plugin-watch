engines:
  eslint:
    enabled: true
    channel: 'eslint-9'
    config:
      config: 'eslint.config.mjs'

ratings:
  paths:
    - '**.js'

exclude_patterns:
  - 'html/jquery.tipsy.js'

checks:
  return-statements:
    enabled: false
  similar-code:
    enabled: false
  file-lines:
    config:
      threshold: 1000
  method-lines:
    config:
      threshold: 100
  method-complexity:
    config:
      threshold: 20
